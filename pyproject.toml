[project]
name = "kb-pente-ai"
version = "0.1.0"
description = "Machine learning for the board game Pente"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "mypy",
    "nvidia-ml-py; sys_platform == 'linux'",
    "numpy",
    "torch",
]
